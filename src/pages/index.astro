---
// Traditional Chinese route. The page body lives in the shared component so
// the two locales cannot drift apart.
import HomePage from "../components/HomePage.astro";
---

<HomePage locale="zh-Hant" />
